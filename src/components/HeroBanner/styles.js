import styled from "styled-components";

const HeroBanner = styled.div`
    width: 600px;
    max-width: 100%;

    img {
        object-fit: contain;
        max-width: inherit;
    }
`

export default HeroBanner